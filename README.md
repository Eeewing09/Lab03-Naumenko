1) Разницы в результатах нет
2) x++ прибавляет к x число после, что позволяет сохранить исходное значение x
++x же, сразу складывает, не давая нам увидеть исходное значение
3) тип bool
4) При "коротком замыкании" среда C#  перестает делать логические вычисления, если bool уже точно будет известна

int itemsCost = 0;
int totalCost = 500;
// Здесь мы проверяем, что делить на ноль нельзя
if (itemsСost > 0 && (totalCost / itemsCost) > 200)
{
    Console.WriteLine("Средняя стоимость товара высокая");
}
5) 10 - (2 * 3) + (4 / 2)
6) Чтобы человек читающий код, ыбстрее понял порядок действий